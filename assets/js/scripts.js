var guyScrollListener = debounce(function() {
    // Enter All scroll functions here
}, 150);
window.addEventListener('scroll', guyScrollListener);


jQuery(document).ready(function($) {

    // Call any code on document ready with JQUERY

});